# Risk AI Q&A — 快速理解代码指南

本文档帮助你在 **30 分钟内** 建立对项目的整体认知。建议按「请求链路」阅读，而不是按包名乱翻。

---

## 1. 先抓三件事（5 分钟）

| 问题 | 去哪看 |
|------|--------|
| 项目是干什么的？ | 项目根目录 `README.md` |
| 有哪些接口？ | `DocController`、`RiskQaController` |
| 用了哪些中间件？ | `pom.xml`、`application.yml`、`docker-compose.yml` |

### 三个对外接口

| 方法 | 路径 | 作用 |
|------|------|------|
| POST | `/doc/ingest` | 上传 TXT/PDF，解析入库 |
| POST | `/risk/qa` | 风控智能问答（RAG） |
| DELETE | `/doc/clear` | 清空知识库向量 |

---

## 2. 按「请求链路」读代码（推荐）

不要从包名乱翻，**顺着一次真实请求**看下去。

### 路径 A：文档入库

```
DocController.ingest()
  → DocumentService.ingest()
      → DocumentParser（Tika 解析 TXT/PDF）
      → TokenTextChunker（800 Token / 150 重叠切片）
      → VectorStoreService.add()（写入 Milvus）
      → Redis 记录 chunk id（供 /doc/clear 使用）
```

**重点文件：**

| 文件 | 说明 |
|------|------|
| `controller/DocController.java` | 接收入参、校验文件 |
| `service/DocumentService.java` | 入库主流程（5 步） |
| `util/DocumentParser.java` | Apache Tika 解析 |
| `util/TokenTextChunker.java` | jtokkit 切片 |
| `service/VectorStoreService.java` | Milvus 向量库封装 |

### 路径 B：风控问答（核心）

```
RiskQaController.qa()
  → @RateLimit（RateLimitAspect 限流）
  → RagQaService.ask()
      ① Redis 缓存命中？→ 直接返回
      ② Milvus 向量检索 → 召回相关片段
      ③ 拼风控 Prompt + 调大模型
      ④ 失败则降级兜底
      ⑤ QaLogService 写 MySQL 日志
```

**重点文件：**

| 文件 | 说明 |
|------|------|
| `controller/RiskQaController.java` | 问答入口 |
| `aspect/RateLimitAspect.java` | AOP 限流切面 |
| `service/RagQaService.java` | **最核心**：RAG + Prompt + 缓存 + 降级 |
| `service/VectorStoreService.java` | 相似度检索 |
| `service/QaLogService.java` | 问答日志落库 |
| `entity/QaLog.java` | 日志表实体 |

### 路径 C：清空知识库

```
DocController.clear()
  → DocumentService.clearAll()
      → 从 Redis 取所有 chunk id
      → VectorStoreService.delete()
      → 删除 Redis 中的 id 集合
```

---

## 3. 四大模块对应关系

```
模块1 知识库入库     → DocumentService + DocumentParser + TokenTextChunker
模块2 RAG 问答       → RagQaService + VectorStoreService
模块3 Redis 能力     → RateLimitAspect / RateLimitService（限流）
                       RagQaService 内 cache / degrade（缓存、降级）
模块4 MySQL 日志     → QaLog + QaLogMapper + QaLogService
```

### 公共支撑层

| 包 | 职责 |
|----|------|
| `common/` | 统一返回 `Result`、业务异常、全局异常处理 |
| `config/` | 跨域、Redis、MyBatis、OpenAPI、`RagProperties` 配置绑定 |
| `dto/` | 请求/响应数据结构 |
| `annotation/` | `@RateLimit` 限流注解 |
| `util/` | Tika 解析、Token 切片、客户端 IP 获取 |

---

## 4. 目录结构速查

```
src/main/java/com/gm/riskaiqa/
├── RiskAiQaApplication.java        启动类
├── annotation/                     @RateLimit
├── aspect/                         RateLimitAspect（限流切面）
├── common/                         Result、异常、全局异常处理
├── config/                         全局配置（CORS、Redis、MyBatis、RagProperties）
├── controller/                     DocController、RiskQaController
├── dto/                            QaRequest、QaResponse、IngestResponse、ReferenceChunk
├── entity/                         QaLog
├── mapper/                         QaLogMapper
├── service/                        业务服务（见上文四大模块）
└── util/                           DocumentParser、TokenTextChunker、WebUtil
```

---

## 5. 配置从哪来

所有可调参数在 `application.yml` 的 `risk-ai.*` 下，由 `config/RagProperties.java` 绑定：

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `chunk.size` / `chunk.overlap` | 800 / 150 | Token 切片与重叠 |
| `rag.top-k` | 5 | 检索返回条数 |
| `rag.similarity-threshold` | 0.5 | 相似度阈值 |
| `cache.ttl-minutes` | 60 | 答案缓存时长 |
| `rate-limit.max-requests` / `window-seconds` | 20 / 60 | 限流阈值 |
| `degrade.fallback-answer` | … | 大模型不可用时的兜底文案 |

大模型、MySQL、Redis、Milvus 的地址和密钥均在 `application.yml` 中通过**环境变量占位**，不在 Java 代码里硬编码，例如：

- `OPENAI_API_KEY`、`OPENAI_BASE_URL`
- `MYSQL_HOST`、`MYSQL_USER`、`MYSQL_PASSWORD`
- `REDIS_HOST`、`REDIS_PORT`
- `MILVUS_HOST`、`MILVUS_PORT`

---

## 6. 总流程图

```mermaid
flowchart LR
    A[前端] --> B[/doc/ingest]
    A --> C[/risk/qa]
    A --> D[/doc/clear]

    B --> E[Tika 解析]
    E --> F[800/150 切片]
    F --> G[Milvus 向量库]
    F --> H[Redis 记录 chunk id]

    C --> I{限流}
    I --> J{缓存命中?}
    J -->|是| K[直接返回]
    J -->|否| L[Milvus 检索]
    L --> M[风控 Prompt + LLM]
    M --> N[写 MySQL qa_log]
    M --> O[写 Redis 缓存]

    D --> P[删 Milvus + 清 Redis]
```

---

## 7. 本地跑起来再对照代码（最快）

```powershell
# 1. 启动中间件
docker compose up -d

# 2. 配置大模型（示例）
$env:OPENAI_API_KEY="sk-xxx"
$env:OPENAI_BASE_URL="https://api.openai.com"

# 3. 使用 JDK 17 启动
$env:JAVA_HOME="F:\tool\jdk-17.0.9"
$env:PATH="$env:JAVA_HOME\bin;$env:PATH"
mvn spring-boot:run
```

然后：

1. 打开 Swagger：<http://localhost:8080/swagger-ui.html>
2. 调用 `POST /doc/ingest` 上传一个 `.txt` 或 `.pdf`
3. 调用 `POST /risk/qa` 提问
4. 对照 `RagQaService.ask()` 看日志或打断点

**跑一遍比干看代码快很多。**

---

## 8. 建议阅读顺序（约 30 分钟）

| 顺序 | 文件 | 时间 | 目的 |
|------|------|------|------|
| 1 | `README.md` | 5 min | 技术栈与启动方式 |
| 2 | `DocController` + `RiskQaController` | 5 min | 三个接口入口 |
| 3 | `RagQaService` | 10 min | 问答核心逻辑 |
| 4 | `DocumentService` | 5 min | 文档入库流程 |
| 5 | `application.yml` + `RagProperties` | 5 min | 可调参数 |

其余 `common`、`config`、`aspect` 等按需阅读。

---

## 9. 深入阅读（按兴趣选读）

### 只关心「问答怎么答出来的」

1. `RiskQaController.qa()`
2. `RagQaService.ask()` — 五步：缓存 → 检索 → LLM → 写缓存 → 写日志
3. `RagQaService.RISK_SYSTEM_PROMPT` — 风控防幻觉 Prompt
4. `VectorStoreService.similaritySearch()`

### 只关心「文档怎么入库的」

1. `DocController.ingest()`
2. `DocumentService.ingest()` — 五步：Tika → 计数 → 切片 → Milvus → Redis
3. `TokenTextChunker.split()` — 800/150 滑窗算法
4. `DocumentParser.parse()` — Tika 统一解析

### 只关心「Redis / MySQL 怎么配合的」

| 能力 | 实现位置 | Redis Key 示例 |
|------|----------|----------------|
| 限流 | `RateLimitAspect` + `RateLimitService` | `risk-ai:qa:rl:risk-qa:{ip}` |
| 答案缓存 | `RagQaService` | `risk-ai:qa:cache:{md5}` |
| chunk 追踪 | `DocumentService` | `risk-ai:doc:chunk-ids`（Set） |
| 问答日志 | `QaLogService` → MySQL `qa_log` | — |

---

## 10. 常见问题

**Q：为什么 Spring Boot 是 3.4.5 而不是 3.2？**  
A：Spring AI 1.0.0 GA 要求 Boot 3.4+；能配 3.2 的 Spring AI 0.8.x 在当前 Maven 镜像环境下载不到。详见 `README.md`「版本说明」。

**Q：风控 Prompt 在哪改？**  
A：`service/RagQaService.java` 中的 `RISK_SYSTEM_PROMPT` 常量。

**Q：切片规则能改吗？**  
A：改 `application.yml` 中 `risk-ai.chunk.size` 和 `risk-ai.chunk.overlap`，无需改代码。

**Q：接口返回格式是什么？**  
A：统一 `{ "code": 200, "message": "success", "data": {...}, "timestamp": ... }`，见 `common/Result.java`。

---

## 相关文档

- [README.md](../README.md) — 项目说明、技术栈、API 示例
- [application.yml](../src/main/resources/application.yml) — 全部配置项
- [schema.sql](../src/main/resources/schema.sql) — `qa_log` 表结构
